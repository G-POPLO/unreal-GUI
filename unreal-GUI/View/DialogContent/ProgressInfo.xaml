<UserControl x:Class="unreal_GUI.View.DialogContent.CompressInfo" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:unreal_GUI.View.DialogContent" 
             mc:Ignorable="d" 
             d:DesignWidth="600">
    <StackPanel Margin="10">
        <!-- 压缩进度 -->
        <TextBlock Text="压缩进度" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        <Grid Height="15" Width="580">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar x:Name="ProgressBar" Grid.Column="0"  VerticalAlignment="Stretch"/>
            <TextBlock x:Name="ProgressText" Grid.Column="1" Text="0%"/>
        </Grid>
        <!-- 当前操作 -->
        <TextBlock x:Name="CurrentOperationText" Text="" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <!-- 压缩信息 -->
        <TextBlock Text="压缩信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        <ScrollViewer Height="200" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="CompressText" TextWrapping="Wrap" IsReadOnly="True" AcceptsReturn="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Opacity="0.8"/>
        </ScrollViewer>
    </StackPanel>
</UserControl>