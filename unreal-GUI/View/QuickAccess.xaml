<UserControl x:Class="unreal_GUI.QuickAccess"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:unreal_GUI"
             xmlns:viewmodel="clr-namespace:unreal_GUI.ViewModel"
            xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             mc:Ignorable="d" 
             d:DesignWidth="800"
             Unloaded="UserControl_Unloaded">
    <UserControl.DataContext>
        <viewmodel:QuickAccessViewModel />
    </UserControl.DataContext>
    <Grid Margin="20">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10">
                <!-- 页面标题 -->
                <TextBlock Text="快速访问" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
                
                <!-- 插件目录卡片 -->
                <Border Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}" 
                        
                          CornerRadius="4" Margin="0,0,0,20">
                    <StackPanel Margin="15">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8B7;" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="插件目录" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        
                        <!-- 根据引擎列表动态生成插件目录按钮 -->
                        <ItemsControl ItemsSource="{Binding Engines}" Margin="0,0,0,10">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Margin="0,0,0,10" Command="{Binding DataContext.OpenPluginDirectoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" 
                                            Style="{DynamicResource AccentButtonStyle}" HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8B7;" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding DisplayName, StringFormat=' {0} 插件目录'}"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                
                <!-- 常用网站卡片 -->
                <Border Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}" 
                        
                          CornerRadius="4" Margin="0,0,0,20">
                    <StackPanel Margin="15">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE71B;" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="常用网站" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        <WrapPanel Orientation="Horizontal">
                            <Button Margin="0,0,10,10" Command="{Binding OpenWebsiteCommand}" CommandParameter="https://www.fab.com/" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE71B;" Margin="0,0,5,0"/>
                                    <TextBlock Text="Fab"/>
                                </StackPanel>
                            </Button>
                            <Button Margin="0,0,10,10" Command="{Binding OpenWebsiteCommand}" CommandParameter="https://dev.epicgames.com/community/unreal-engine/learning" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE71B;" Margin="0,0,5,0"/>
                                    <TextBlock Text="社区学习库"/>
                                </StackPanel>
                            </Button>
                            <Button Margin="0,0,10,10" Command="{Binding OpenWebsiteCommand}" CommandParameter="https://docs.unrealengine.com/" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE736;" Margin="0,0,5,0"/>
                                    <TextBlock Text="文档"/>
                                </StackPanel>
                            </Button>
                            <Button Margin="0,0,10,10" Command="{Binding OpenWebsiteCommand}" CommandParameter="https://blueprintue.com/" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE736;" Margin="0,0,5,0"/>
                                    <TextBlock Text="BlueprintUE"/>
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                    </StackPanel>
                </Border>
                
                <!-- 自定义文件夹卡片 -->
                <Border Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}" 
                        
                          CornerRadius="4">
                    <StackPanel Margin="15">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8B7;" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="自定义文件夹" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        
                        <ItemsControl ItemsSource="{Binding CustomButtons}" Margin="0,0,0,15">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Margin="0,0,0,10" Command="{Binding DataContext.OpenCustomDirectoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" HorizontalAlignment="Left">
                                        <StackPanel Orientation="Horizontal">
                                            <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8B7;" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding DisplayName}"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <StackPanel Orientation="Horizontal">
                            <Button Margin="0,0,10,0" Command="{Binding AddCustomCommand}" 
                                    Style="{DynamicResource AccentButtonStyle}" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE710;" Margin="0,0,5,0"/>
                                    <TextBlock Text="添加"/>
                                </StackPanel>
                            </Button>
                            <Button Margin="0,0,0,0" Command="{Binding DeleteCustomCommand}" 
                                    Style="{DynamicResource AccentButtonStyle}" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <ui:FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE74D;" Margin="0,0,5,0"/>
                                    <TextBlock Text="删除"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
