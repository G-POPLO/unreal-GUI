<UserControl x:Class="unreal_GUI.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:unreal_GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20,20,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" CanContentScroll="True" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="Title" TextWrapping="Wrap" FontWeight="Bold" FontSize="15" Text="设置" Margin="10,10,10,10"/>
                <TextBlock x:Name="text" TextWrapping="Wrap" Margin="10,0,10,0" Text="设定引擎目录"/>
                <ListBox x:Name="EnginePathsList" Height="100" Margin="10,10,10,10"/>
                <StackPanel Orientation="Horizontal" Margin="10,10,10,10">
                    <Button Content="新增" Margin="0,0,10,0" Click="AddEnginePath_Click"/>
                    <Button Content="删除" Margin="10,0,10,0" Click="RemoveEnginePath_Click"/>
                    <Button Content="自动设置" Margin="10,0,10,0" Click="AutoPath_Click"/>
                    <Button Content="保存路径列表" Margin="10,0,10,0" Click="SavePaths_Click"/>
                </StackPanel>
                <TextBlock x:Name="Msg" TextWrapping="Wrap" Margin="10,0,20,0" Visibility="Hidden"><Run Text="输入"/><Run Language="zh-cn" Text="Remon目录"/><LineBreak/><Run Language="zh-cn"/></TextBlock>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
