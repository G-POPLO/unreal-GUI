<UserControl x:Class="unreal_GUI.QuickAccess"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:unreal_GUI"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <StackPanel>
                <TextBlock TextWrapping="Wrap" FontSize="18" Text="快速访问" Height="23" Width="760" Margin="10,10,10,10" HorizontalAlignment="Left" VerticalAlignment="Top" FontWeight="Bold"/>
                <Separator/>
                <TextBlock TextWrapping="Wrap" FontSize="14" Height="23" Width="760" Margin="10,10,10,10" Text="打开插件目录"/>
                <ItemsControl ItemsSource="{Binding Engines}" Margin="10,0,10,5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding DisplayName}" Margin="0,5,0,5" Command="{Binding DataContext.OpenPluginDirectoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Separator/>

                <TextBlock TextWrapping="Wrap" FontSize="14" Height="23" Width="760" Margin="10,10,10,10" Text="常用网站访问"/>
                <StackPanel x:Name="WebSite_Panel" Margin="10,0,10,10" Orientation="Horizontal">
                    <Button Content="Fab官网" Command="{Binding OpenWebsiteCommand}" CommandParameter="https://www.fab.com/"/>
                    <Button Content="Unreal Engine学习库" Command="{Binding OpenWebsiteCommand}" CommandParameter="https://dev.epicgames.com/community/unreal-engine/learning" Margin="10,0,0,0"/>
                </StackPanel>
                <Separator/>
                <TextBlock TextWrapping="Wrap" FontSize="14" Height="23" Width="760" Text="自定义文件夹" Margin="10,0,9,0"/>
                <TextBlock TextWrapping="Wrap" FontSize="12" Width="760" Text="可将常用的文件夹放在这里方便打开" Margin="10,0,10,0
                           " VerticalAlignment="Center"/>
                <StackPanel x:Name="Recommand_Panel" Orientation="Horizontal" Margin="10,0,10,0">
                    <TextBlock TextWrapping="Wrap" Text="文件管理器窗口太多？试试ExplorerTabUtility，将所有窗口整合到同一个文件管理器" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <Button Content="前往下载" Command="{Binding OpenWebsiteCommand}" CommandParameter="https://github.com/w4po/ExplorerTabUtility/releases/" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8,0,0,0" FontSize="10" RenderTransformOrigin="0.5,0.5"/>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding CustomButtons}" Margin="10,0,10,5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding DisplayName}" Margin="0,5,0,5" Command="{Binding DataContext.OpenCustomDirectoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <StackPanel Orientation="Horizontal" Margin="10,10,10,10">
                    <Button Content="添加" Command="{Binding AddCustomCommand}"/>
                    <Button Content="删除" Margin="10,0,0,0" Command="{Binding DeleteCustomCommand}"/>
                </StackPanel>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
